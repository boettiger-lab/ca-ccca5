apiVersion: v1
kind: Service
metadata:
  name: ca-plant-heat
  namespace: biodiversity
  labels:
    app: ca-plant-heat
spec:
  type: ClusterIP
  selector:
    app: ca-plant-heat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
